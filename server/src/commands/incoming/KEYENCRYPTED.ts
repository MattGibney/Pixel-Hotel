import { Command } from '../../commandFactory';

/**
 * This is related to the use of encrypted traffic. We're not using it at the
 * moment. So this is a NOOP.
 */
export default function KEYENCRYPTED(props: Command) {
  // NOOP
}
